body { font-family: <%= @custom.font.humanize %>; }

body.posts .navbar-header, body.posts footer,
.posts .post { background-color: <%= @custom.color_value %>; }
.navbar-header {
  border-bottom: 1px solid #ddd;
}
